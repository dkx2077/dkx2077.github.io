# 浏览器检查与发布记录（2026-09-05）

## 测试边界

本次使用实际云端 Chrome 浏览器访问本地生产构建。浏览器报告 `GL_VENDOR = Disabled, GL_RENDERER = Disabled`，WebGL2 上下文无法创建，因此**没有完成 3D 画面、GPU 帧率、触屏硬件或实体手机验收**。没有将阅读模式截图称为 3D 渲染截图，也没有用模拟渲染器冒充 GPU 测试。

网站正常执行了 WebGL 失败降级，全部正文和链接保留。用户明确要求正式部署，本次继续完成可执行的浏览器检查与现有 GitHub Pages 发布流程。

## 实际通过的浏览器检查

| 检查                                   | 结果                                                      |
| -------------------------------------- | --------------------------------------------------------- |
| 桌面首屏，WebGL 上下文创建失败         | 自动显示完整阅读页及明确提示，无永久加载遮罩              |
| 390 × 844 阅读布局                     | 标题、导航、正文可读，无横向溢出                          |
| 320 × 720 窄屏                         | 导航在边界内，无横向溢出                                  |
| 768 × 1024 平板视口                    | 无横向溢出                                                |
| 844 × 390 横屏视口                     | 无横向溢出                                                |
| 1440 × 900 桌面视口                    | 无横向溢出                                                |
| Research 导航                          | 跳转 Publications 正文                                    |
| Ongoing work 导航                      | 跳转对应栏目，空内容说明正常                              |
| 论文与链接                             | 11 篇论文、24 个已标记链接保留                            |
| 精选论文出版页、预印本、项目和代码链接 | href 正确，外部链接包含 `_blank` 与 `noopener noreferrer` |
| 页尾仓库链接                           | 指向现有 dkx2077/dkx2077.github.io 仓库                   |

视口尺寸通过开发环境的同源 iframe 测试页设置；实际内容区扣除了桌面浏览器滚动条宽度。这是响应式布局检查，不能替代移动 Safari、Android Chrome、触屏或设备 GPU 仿真。没有发送邮件、提交表单或人工制造 Umami 生产事件。

## 自动回归

`npm test` 验证 14 项检查，包括固定机位水平角跨界、俯仰限制、触摸 PointerEvent 拖动误点抑制、键盘复位、阅读面板焦点与 Escape、无 WebGL/模块加载失败/拒绝存储的降级，以及原有 Umami 的事件和隐私边界。这里的触摸检查是事件模拟，不是实体手机操作。

## 可复现的本地预览

```bash
npm ci
npm run build
npm run dev
```

Vite 仅服务 `dist/` 的生产构建；`/__qa` 提供视口测试界面。测试界面由开发中间件提供，不进入 GitHub Pages 部署产物。源码改动后重新构建即可检查新版本。

尚待后续在支持 WebGL2 的实体设备上核验：霓虹与建筑对齐、360° 环视、上下限制、招牌点击、阅读模式返回、实际触控和持续帧率；桌面 200% 缩放与不同浏览器也仍需单独验收。

## 霓虹美术第二版复核

本轮继续保留上述实机测试边界，没有将 CPU 几何检查当作 GPU 画面验收。自动回归增加至 17 项，已验证：

- DOM 招牌宽高与真实 Three.js 搪瓷底板对应；字面位于底板前方。
- 转向时屏幕外链接同步隐藏并退出键盘导航，回到对应街区后恢复。
- 高／低画质切换同时改变雨滴 drawRange 和 CPU 更新范围，暂停时不更新雨滴。
- 近景重复构件保留实例化，场景基础绘制次数低于 25（不含招牌、后处理）。
- 初始姓名招牌在 320×844、360×915、390×844、768×1024、844×390、1440×900 的水平投影均完整；窄长屏视场角根据宽高比自适应。

独立只读复核使用真实场景几何，在各招牌上取 9 点进行 CPU 射线检查，未发现现有建筑遮挡；字体度量确认当前英文标题能放入面板。保留 ACES / sRGB 输出流程。静态检查未发现地面 shader 语法问题，但尚未在可用 GPU 上编译或观测实际画面。

额外修复了实例对象销毁时的专属 GPU 缓冲释放，以及减少动态模式中远景加载完成后未补渲染的问题。

## Awards / Ongoing 建筑补充与大学竖牌移除

- 移除大学竖牌的 HTML、场景布局、实体特例及 CSS；正文教育信息保持原样。
- 南、西街区各增加四栋塔楼和连桥，西南转角新增一栋近景楼。复用现有全景，无新增网络素材或点光源。
- 修正南、西远楼窗户朝外的问题，按各自立面宽／深设置窗格，跳过固定观察点不可见的面。
- 18 项自动检查通过。新增回归直接读取 Three.js 实例矩阵，确认所有远楼窗户在朝向观察点的立面边界内，四个象限均有亮窗；原有绘制次数、低画质和窄屏检查继续通过。
- CPU 射线复核未发现新增建筑遮挡现有招牌。更密的采样发现少量既有店檐／其他招牌外壳与牌面边缘交叠，本轮不改变原有招牌位置。连桥后移 0.8 米，增加两端与楼体的接入深度。

以上为代码、几何及构建检查；当前环境仍无法完成 GPU 画面或实体手机帧率验收。

## 建筑照明与装饰字幕清理

移除顶部两组中英文装饰字幕。新增独立照明模块，调整主墙材质、反方向主光、高层泛光、环境反射和高画质静态阴影；保持曝光及栏目文字表现。

19 项自动检查通过。新回归验证南／西墙面接受主灯直射，高层代表点落入泛光锥体，窗片不投影，Low／High 切换与阴影目标释放正常。独立 CPU 复核确认八栋新增塔楼的上层均在对应泛光范围内，距原点 50m 内的实体几何全部被阴影相机覆盖；最外圈少数楼体边缘超出阴影范围。

环境反射使用一次生成的 128px PMREM，高画质阴影为缓存的 2048px 单灯 PCF。未新增外部图片请求。当前云端 WebGL 仍不可用，尚未完成 PMREM、阴影与最终画面的 GPU 验收或实体设备帧率测试。

## 霓虹夜景美术收敛

针对建筑偏白、偏亮的反馈，压低基础照明、收窄彩光覆盖与反射环境，调整灯管源色和曝光，并同步收敛招牌光晕、窗灯、雨与地面反光。保留建筑结构、交互和静态阴影策略。未增加资源下载、灯数量或渲染通道。

继续执行原有 19 项自动回归及生产构建；颜色与光照数值分析属于源码检查，不能等同于画面已在 GPU 上验收。当前环境仍无法进行 GPU 实机画面或帧率测试。

## 远景贴图协调性优化

以用户提供的实机截图为视觉依据，替换了写实程度与亮度过高的第二版全景。新图采用简洁深靛蓝楼群和稀疏窗灯；已直接查看生成原图与部署用 WebP。WebP 为 2172 × 724、37,340 字节，比旧图减少约 89%。

19 项原有自动回归与生产构建通过。补充的独立 CPU 检查确认圆柱三次重复保持 3:1 比例、4,608 个三角形、透明度范围有效、上下及环绕边界透明；加载前和加载失败时隐藏背景层，静态模式成功加载会补帧，销毁后的迟到贴图被释放。生产构建引用新图，旧图已移出产物。

以上依据为用户截图、素材查看、源码／几何检查和构建结果；当前云端浏览器 WebGL 禁用，未完成改动后的 GPU 画面或实体手机帧率验收。

## 移动阅读与触控优化

根据用户的 iPhone 阅读截图，重排论文的刊会标签、标题、作者及操作链接，收紧章节留白，保留所有 11 篇论文和 24 个已标记链接。移除装饰表情箭头，链接使用独立、可换行的 44px 高按钮；阅读首屏复用原有轻量城市图。

触屏首次访问默认阅读，不初始化 3D；显式选择与已保存的 city 偏好仍可进入城市。触屏及窄屏正文面板改为文档原生滚动，移除手机的固定嵌套滚动容器；横竖屏触屏均沿用这一策略。保留双指缩放，阅读时暂停 3D 控制，调整横向拖动灵敏度。恢复阅读位置使用即时滚动，避免焦点跳回页首；主动进入 3D 不再被旧阅读锚点立即拉回面板。

本轮 21 项自动回归通过，新增触屏默认模式、主动进入与保存偏好、原阅读位置恢复检查，并扩展暂停控制和论文语义标题检查。独立内容检查确认全部 11 个论文标题与原始版本一致，原有论文／个人主页链接地址未改变。

实际云端 Chrome 布局与交互检查：

- 320×720、390×844、768×1024、844×390、1440×900 阅读页均无横向溢出（内框实际内容宽度扣除桌面滚动条）。
- 390px 内框、200% 根字号时无横向溢出；导航换行，姓名、栏目标题和正文不重叠。此项是文字放大，不是模拟浏览器缩放或真实 iPhone 字号设置。
- Research 锚点跳转后正确高亮，标题位于顶部导航下方；导航高度变化会同步更新锚点避让。
- 面板布局检查中，390px 正文为 relative / overflow:visible；一次滚动后文档 scrollTop 为 640、面板 scrollTop 仍为 0，确认滚动由文档承担。返回工具栏保持 top:0，关闭按钮正常隐藏面板。
- 直接查看了普通论文卡片、放大文字和滚动中面板的浏览器截图。

面板检查通过仅限本地的 HTML/CSS 布局状态；默认模式与 3D 交互的自动测试使用已明确标识的测试替身。云端 WebGL 仍被禁用，没有完成真实 3D 画面、iPhone Safari 手势／惯性滚动或实体 GPU 帧率验收。

## Scholar 招牌、增强雨景与局部水滩

只替换截图中东南侧的小姓名牌，首页大姓名牌与全部个人正文不变。Google Scholar 使用原有 `WsJD-ukAAAAJ` 个人地址，真实 HTML 可编辑；字体度量确认 52px Rajdhani 标题含字距约 349px，低于面板 498px 的内容宽度。新标签页与隐私属性保留，点击仍为单次 `profile_click / google_scholar` 事件。

新增 12 块不规则水面，共享一个 512px、无 MSAA 的 Reflector 目标；加入雨滴扩散环、法线扰动、边缘渐隐和反射柔化。高／低档雨线分别为 1,800 / 560，带颜色衰减尾迹，Low 仅更新和上传有效范围。水面位置按镜像光源的实际落点布置，CPU 检查覆盖 About、Featured、Research、Awards、Work、Contact、Scholar 上下灯轨中心的反射落点。

本轮 28 项自动回归通过，其中 6 项水面检查使用真实 Three.js 几何及原生 Reflector 的镜像相机／裁剪计算，渲染边界为明确标注的 CPU 测试替身：

- 所有水面朝上，合并为一个网格与目标；多数广场铺装不被水面覆盖。
- 镜像相机高度、投影裁剪、图像与矩阵配对、捕获限频、异步补帧、离开视锥时跳过捕获。
- 初始 Low 不创建全尺寸目标；降档释放目标、停止捕获，回到 High 恢复；销毁移出场景且不重复释放。
- 浮点附件不支持时使用字节目标；捕获异常后恢复主渲染器状态，降为轻量水纹且不逐帧重试。
- 暂停／减少动态模式冻结水面时间，按需环视强制刷新静态倒影；仍无独立水面动画循环。

原有移动阅读、原生滚动、拖动误点抑制、可编辑招牌、原有论文链接和统计隐私回归均保持通过。新增水面模块、world 集成及新测试的 ESLint 检查通过；较大范围 ESLint 仍报告基线既有的 7 处引号／缩进与 Prettier 规则冲突，已与修改前版本核对，本轮未改动这些无关格式配置。

当前云端 WebGL 禁用，因此本轮没有最终 shader GPU 编译、实际倒影截图、Safari/Android 触屏或帧率验收。生产构建和 CPU 回归不能代替这些检查；CSS3D 字形不进入水面捕获，实际倒影对象为 WebGL 建筑、窗格、招牌底板和灯轨。开源选型与限制详见 [雨水与反射实现](RAIN-REFLECTIONS.md)。
